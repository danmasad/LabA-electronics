# Brewfile — toolchain for building Lab A reports (Markdown -> PDF).
#
# Install everything with:
#   brew bundle
#
# Run it from the folder containing this file (the repo root).

brew "pandoc"     # Markdown -> LaTeX
brew "tectonic"   # LaTeX -> PDF engine; auto-fetches TeX packages/fonts on first run
